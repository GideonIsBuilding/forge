# Normalize line endings to LF in the repository for all text files.
# On checkout, Windows gets CRLF (autocrlf); Linux/Mac get LF.
* text=auto eol=lf

# Explicitly binary — never touch line endings
*.png binary
*.bin binary
*.db  binary
