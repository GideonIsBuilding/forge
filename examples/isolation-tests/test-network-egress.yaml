name: test-network-egress
version: 1.0.0
dependencies: []
jobs:
  build:
    runtime: alpine:3.18
    resources:
      cpu: 0.5
      memory: 128Mi
    steps:
      - name: egress
        run: "wget -T 5 -q https://google.com -O /dev/null && echo 'ESCAPED' || echo 'BLOCKED'"
artifacts: []
